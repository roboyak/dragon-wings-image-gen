<!DOCTYPE html>
<html lang="en" class="dark">
  <head>
    <title>Dragon Wings Solar AI Image Generator</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="description" content="Generate AI images with Dragon Wings - powered by Stable Diffusion">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <!-- Dragon Wings Design System Fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&family=JetBrains+Mono:wght@400;500&family=Space+Grotesk:wght@500;600;700&display=swap" rel="stylesheet">

    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "dragon_wings_design_system", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="min-h-screen" style="background-color: hsl(var(--background)); color: hsl(var(--foreground));">
    <%# Skip to main content link for accessibility - hidden until focused %>
    <a href="#main-content" class="sr-only focus:not-sr-only focus:absolute focus:top-0 focus:left-0 focus:z-50 focus:px-4 focus:py-2 focus:bg-cyan-600 focus:text-white focus:font-semibold focus:rounded-br-lg">Skip to main content</a>

    <% if user_signed_in? %>
      <%# Authenticated Layout: Sidebar + Main Content %>
      <div class="flex min-h-screen">
        <%# Sidebar Navigation %>
        <%= render 'shared/sidebar' %>

        <%# Main Content Area %>
        <main id="main-content" class="flex-1 flex flex-col min-h-screen">
          <%# Prompt Bar Header - Always visible at top %>
          <%= render 'shared/prompt_bar' %>

          <%# Flash Messages %>
          <% if notice %>
            <div class="bg-green-900/20 border-l-4 border-green-500 text-green-400 p-4" role="alert">
              <p><%= notice %></p>
            </div>
          <% end %>
          <% if alert %>
            <div class="bg-red-900/20 border-l-4 border-red-500 text-red-400 p-4" role="alert">
              <p><%= alert %></p>
            </div>
          <% end %>

          <%# Page Content %>
          <div class="flex-1 overflow-y-auto">
            <%= yield %>
          </div>
        </main>
      </div>
    <% else %>
      <%# Guest Layout: No Sidebar %>
      <% if notice %>
        <div class="bg-green-900/20 border-l-4 border-green-500 text-green-400 p-4" role="alert">
          <p><%= notice %></p>
        </div>
      <% end %>
      <% if alert %>
        <div class="bg-red-900/20 border-l-4 border-red-500 text-red-400 p-4" role="alert">
          <p><%= alert %></p>
        </div>
      <% end %>

      <%= yield %>
    <% end %>
  </body>
</html>
