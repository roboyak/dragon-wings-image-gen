<!DOCTYPE html>
<html class="dark">
  <head>
    <title>Dragon Wings AI Image Generator</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "dragonsbreath", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <script>
      // Dark mode toggle - Load theme from localStorage
      const theme = localStorage.getItem('theme') || 'dark';
      if (theme === 'dark') {
        document.documentElement.classList.add('dark');
      } else {
        document.documentElement.classList.remove('dark');
      }
    </script>
  </head>

  <body class="min-h-screen" style="background-color: hsl(var(--background)); color: hsl(var(--foreground));">
    <!-- Flash Messages (Dark Mode Compatible) -->
    <% if notice %>
      <div class="bg-green-900/20 border-l-4 border-green-500 text-green-400 p-4" role="alert">
        <p><%= notice %></p>
      </div>
    <% end %>
    <% if alert %>
      <div class="bg-red-900/20 border-l-4 border-red-500 text-red-400 p-4" role="alert">
        <p><%= alert %></p>
      </div>
    <% end %>

    <%= yield %>
  </body>
</html>
